<section id="contact" class="w-full md:px-10 px-8 md:py-32 py-12 bg-accent">
    <!-------------------------------------------------->
    <!-- WRAPPER -->
    <div  class="w-full max-w-5xl mx-auto">
        <!-------------------------------------------------->
        <!-- TITLE -->
        <h2 class="headline-2 | text-white mb-10">Contacto</h2>

        <!-------------------------------------------------->
        <!-- CONTET -->
        <!-- <form id="formulario-email" class="flex flex-col md:gap-10 gap-4 relative z-20 bg-white md:px-10 px-5 md:py-10 pt-10 pb-5 rounded-lg">
            <div class="flex flex-col">
                <label for="nombre" class="opacity-70 font-bold mb-2 ">Tu nombre:</label>
                <input id="nombre" type="text" class="text | border focus:outline-accent px-4 py-2" required>
            </div>

            <div class="flex flex-col">
                <label for="titulo" class="opacity-70 font-bold mb-2">Titulo del mensaje:</label>
                <input id="titulo" type="text" class="text | border focus:outline-accent px-4 py-2" required>
            </div>

            <div class="flex flex-col">
                <label for="email" class="opacity-70 font-bold mb-2">El mensaje:</label>
                <textarea id="mensaje" cols="30" rows="10" class="text | border focus:outline-accent px-4 py-2 h-32 resize-none" required></textarea>
            </div>


            <button type="submit" class="flex justify-center items-center gap-2 border-2 border-secondary px-5 py-2.5 bg-secondary text-white rounded-lg hover:scale-105 hover:shadow-lg hover:bg-transparent hover:text-secondary active:scale-100 active:shadow-none transition-all duration-200 ease-linear cursor-pointer">
                <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icon-tabler-send" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                    <path stroke="none" d="M0 0h24v24H0z" fill="none"></path>
                    <path d="M10 14l11 -11"></path>
                    <path d="M21 3l-6.5 18a.55 .55 0 0 1 -1 0l-3.5 -7l-7 -3.5a.55 .55 0 0 1 0 -1l18 -6.5"></path>
                </svg>
                Enviar
            </button>
        </form> -->

        <!-- OPCION 2 -->
        <div class="flex md:flex-row flex-col items-center md:gap-10 gap-4 relative z-20 bg-white md:px-10 px-5 md:py-14 py-10 rounded-lg">
            <div class="text">
                Envianos un correo con tus preguntas: <br>
            </div>
            <div id="text-email" class="relative font-bold md:text-4xl text-2xl text-secondary md:py-4 py-2 px-2 md:px-4 border-2 border-dashed md:flex-1 text-center border-accent">
                <span class="underline-soft | cursor-pointer">info@tremenda.web</span>

                <div id="email-tooltip" class="absolute top-[110%] left-1/2 -translate-x-1/2 bg-secondary/70 text-white px-4 py-2 text-sm whitespace-nowrap opacity-0">Email copiado ✔</div>
            </div>
        </div>


    </div>

</section>

<style>
    #contact {
        --dot-bg: #F50187;
        --dot-color: rgb(255 255 255 / 10%);
        --dot-size: 5px;
        --dot-space: 80px;
        background:
            linear-gradient(90deg, var(--dot-bg) calc(var(--dot-space) - var(--dot-size)), transparent 1%) center / var(--dot-space) var(--dot-space),
            linear-gradient(var(--dot-bg) calc(var(--dot-space) - var(--dot-size)), transparent 1%) center / var(--dot-space) var(--dot-space),
            var(--dot-color);
    }

    @media (max-width: 768px) {
        #contact {
            --dot-size: 2px;
            --dot-space: 30px;
        }
     }
</style>

<script>
    const textEmail = document.getElementById('text-email');
    let isClickEnabled = true;

    let tl = gsap.timeline();


    textEmail?.addEventListener('click', () => {
        // Prevent second click in 1s
        if(isClickEnabled) {
            isClickEnabled = false

            // Save in clipboard
            navigator.clipboard.writeText('info@tremenda.web')
                .then(() => {
                    console.log('Texto copiado al portapapeles')
                })
                .catch(err => {
                    console.error('Error al copiar al portapapeles:', err)
                })
    
            // Tooltip animation
            tl.fromTo('#email-tooltip', {
                y: 50,
                opacity: 0,
                ease: 'bounce.out',
            },{
                y: 0,
                opacity: 1,
                ease: 'bounce.out',
            });

            tl.to('#email-tooltip', {
                delay: 2,
                y: 50,
                opacity: 0,
            });
            
            // Enable click in 1s
            setTimeout(() => {
                isClickEnabled = true;
            }, 3000);
        }
    })




    // const formulario = document.getElementById('formulario-email');
    // let nombre = '';
    // let titulo = '';
    // let mensaje = '';
    // formulario?.addEventListener('submit', (e) => {
    //     e?.preventDefault();

    //     nombre = e.target.nombre.value;
    //     titulo = e.target.titulo.value;
    //     mensaje = e.target.mensaje.value;

    //     formulario.setAttribute('action', `mailto:adq21791@gmail.com?subject=${titulo}&body=${mensaje}`);

    //     formulario.submit();

    // })
    


</script>
---
import Carousel from "./Carousel.astro";

---
<section id="clients" class="flex flex-col relative w-full md:pt-10 md:pb-32 pb-20 py-14">
    <!-------------------------------------------------->
    <!-- TITLE -->
    <h1 class="headline-2 | text-secondary w-full max-w-5xl mx-auto md:px-10 px-8">Clientes</h1>

    <!-------------------------------------------------->
    <!-- CARROUSEL -->
    <div class="overflow-hidden relative h-96 mt-14">
        <Carousel />
    </div>
</section>

<style>
    /* #clients {
        --dot-bg: #D9D9D9;
        --dot-color: rgb(0 0 0 / 20%);
        --dot-size: 5px;
        --dot-space: 80px;
        background:
            linear-gradient(90deg, var(--dot-bg) calc(var(--dot-space) - var(--dot-size)), transparent 1%) center / var(--dot-space) var(--dot-space),
            linear-gradient(var(--dot-bg) calc(var(--dot-space) - var(--dot-size)), transparent 1%) center / var(--dot-space) var(--dot-space),
            var(--dot-color);
    }

    @media (max-width: 768px) {
        #clients {
            --dot-size: 2px;
            --dot-space: 30px;
        }
     } */
</style>